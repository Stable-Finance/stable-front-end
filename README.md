## Stable Monorepo

This contract holds the frontend for Stable Protocol. It is hosted at [https://app.trystable.com](app.trystable.com). Deployments are automatic using Cloudflare Pages and are triggered automatically on pushing to main on GitHub.

Intstall Dependencies: `pnpm install`
Run locally: `pnpm dev`
